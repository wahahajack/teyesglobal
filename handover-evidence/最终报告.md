# teyesglobal 追踪修复 · 实施报告（Part A + Part B）

- 执行日期：2026-08-05
- 任务依据：`teyesglobal追踪修复-KimiWork实施指令.md`（v3.1，保持未跟踪、未入库）
- 执行方式：Part A 本地代码修复 + Draft PR；Part B 通过 WebBridge 操作用户本地浏览器（Chrome，登录账号 accjack@gmail.com）
- 铁律遵守：未点 GTM「提交/发布」、未改 Ads 出价与预算、未删除/暂停任何现有规则或转化操作、未把 Conversion Label 写入网站代码

---

## 0. 关键标识符

| 项目 | 值 |
|---|---|
| 仓库 | `wahahajack/teyesglobal`，分支 `fix/tracking-conversion-dedup` |
| Draft PR | [#12 fix(tracking): 落地页转化去重与死代码清理](https://github.com/wahahajack/teyesglobal/pull/12)（open / draft / 未合并） |
| Google Ads | 账号 112-215-1801，转化 ID `17816659109` |
| GTM | 容器 `GTM-MSPH5TMK`（账号 6334869722 / 容器 241127555），工作区 `主站转化-v28`（id 29） |
| GA4 | 衡量 ID `G-VBNCN089DG` |
| GTM 备份 | `GTM-MSPH5TMK_v27-backup.json`（12 tags / 7 triggers，本目录） |

---

## Part A 摘要（代码修复，已完成）

- 分支 `fix/tracking-conversion-dedup`，提交 `c6a60c2`，共 6 个文件（+484 / -76）：
  - `public/android-car-stereo-wholesale/script.js`：删直接 `generate_lead` 调用；参数数组补 `gbraid`/`wbraid`
  - `public/android-car-stereo-wholesale/thank-you.html`：删底部内联死代码块（lead_submitted / 假 AW-XXXX / fbq）
  - `public/android-car-stereo-oem-manufacturer/script.js`：格式化后删重复 `generate_lead`、`form_submission`、gtag 直调 `form_submit_attempt`；补 `gbraid`/`wbraid`
  - `public/teyes-android-car-stereo-distributor/index.html`：catalog 意向跳 `thank-you-catalog.html`；删直接 `generate_lead`；删 `trackWhatsAppClick()` 及 onclick；补 `gbraid`/`wbraid`
  - 新增静态契约测试（断言无 `AW-XXXX` / `fbq` / `lead_submitted` / `trackWhatsAppClick` 等，且三页参数数组含 `gbraid`/`wbraid`）
  - `.gitignore` 增加 `handover-evidence/`
- 验证：`npm run lint` / `npm test`（14/14）/ `npm run build` / `git diff --check` 全部通过
- PR 描述已附第 4 节验收等式。**PR 尚未合并，合并即触发部署，由用户决定时机**

---

## Part B · 1 — Google Ads 现有转化操作清单 + TA1D 状态

证据：`5.1-ads-conversions-list.png`（摘要页，统计区间 2026-07-06 ~ 08-04）

| 转化操作 | 目标类别 | 跟踪状态 | 操作优化设置 | 统计方式 | 点击型转化时间范围 | 近 30 天转化 |
|---|---|---|---|---|---|---|
| Submit lead form (Android-Car-**wholesale**) | 提交潜在客户表单 | 最近无转化 | **主要** | **仅一次** | 90 天 | 0.00 |
| Submit lead form (Android-Car-**Distributor**)（TA1D 对应） | 提交潜在客户表单 | 需要注意 | **主要** | **仅一次** | **90 天** | 0.00 |
| Submit lead form (Android-Car-**Manufacturer**) | 提交潜在客户表单 | 最近无转化 | **主要** | **仅一次** | 90 天 | 0.00 |
| Event Click to call | 来电潜在客户 | 需要注意 | 主要 | 仅一次 | 30 天 | 0.00 |
| WhatsApp | 联系人 | 有效（近 30 天 18 次） | 主要 | 仅一次 | 30 天 | 18.00 |

**TA1D 结论：经销商转化为「主要操作（Primary）」+ 计数「一次」，点击窗口 90 天。**

⏸ **Catalog 剥离决策留给用户**（指令 5.1-2）：目前 catalog 与经销商申请共用 TA1D 这一个转化。是否把 catalog 剥离为单独转化/次要操作，等用户拍板后再执行，本次未做任何剥离动作。

---

## Part B · 2 — 新建主站转化操作

证据：`5.1-ads-create-conversion-step.png`、`5.1-ads-new-conversion-label.png`

- 名称：`主站联系表单提交`
- 类别：提交潜在客户表单（Submit lead form）；来源：网站；手动创建
- 计数：**一次（One）**；点击后转化时间范围：**30 天**；主要操作
- **完整 ID + Label：`AW-17816659109/I6gXCOuoitwcEKXJ0q9C`**
- 该 Label 仅配置在 GTM 标签中，未写入任何网站代码（符合铁律 5）

---

## Part B · 3 — GTM 工作区草稿（未发布）

证据：`5.2.8-workspace-draft.png`、`5.2.8-changes-list.png`（概览计数：1 已修改 / 4 已添加 / 0 已删除）

工作区 `主站转化-v28`（id 29）内共 **5 项改动**，全部处于草稿态：

| # | 名称 | 类型 | 更改 | 对应指令步骤 |
|---|---|---|---|---|
| 1 | `CE - form_submit_success - 主站` | 触发器 | 已添加 | 5.2.2（自定义事件 form_submit_success + 3 条 Page Path 不包含条件） |
| 2 | `GAds Tracking \| 主站联系表单提交` | 代码 | 已添加 | 5.2.3（Ads 转化 ID 17816659109 + 新 Label，挂触发器 #1） |
| 3 | `Google Analytics GA4 Event generate_lead Tag` | 代码 | **已修改** | 5.2.4（原 3 个触发器保留，**加挂**触发器 #1，共 4 个） |
| 4 | `CE - contact_email_click` | 触发器 | 已添加 | 5.2.5（自定义事件 contact_email_click） |
| 5 | `Google Analytics GA4 Event contact_email_click Tag` | 代码 | 已添加 | 5.2.5（GA4 事件，Measurement ID G-VBNCN089DG，挂触发器 #4） |

过程证据：`5.2.1-*`（新建工作区）、`5.2.2-*`（触发器）、`5.2.3-*`（Ads 标签）、`5.2.4-*`（generate_lead 加挂后 4 触发器）、`5.2.5-*`（email_click 触发器+标签）。

**关于 5.2.6 增强转化的执行方式变更（已与用户确认）**：新版 GTM 的 Ads 转化标签界面与 Google 代码（AW-17816659109）配置中均**没有**增强转化开关（完整字段已逐项核对）。改为在 **Google Ads 端**完成：用户已逐一确认 4 个表单转化（wholesale / Distributor / Manufacturer / 主站联系表单提交）的「为此转化操作使用增强型转化」**全部勾选**。GTM 侧未做任何多余改动（工作区改动数保持 5）。

未触碰：三条落地页路径规则、WhatsApp（wa.me）规则、电话（tel:）规则、GA4 配置标签——均原样。

⚠ 两个发布前注意事项：
1. **Default Workspace（id 28）里有他人约 1 个月前留下的未发布草稿触发器 `contact_email_click`（trigger id 41）**，与本次新建的 `CE - contact_email_click` 不是一个东西。本次发布主体仅为工作区 `主站转化-v28`（id 29）的 5 项改动，不要混淆、不要从 Default Workspace 发布。
2. 工作区概览页显示「容器质量：紧急（1 个 issue）」提示，内容未点开核实，发布前建议人工看一眼是否与本次改动相关。

---

## Part B · 4 — GA4 增强型衡量开关（✅ 已于 2026-08-05 14:40 补做完成）

证据：`5.3-stream-detail-check.png`（数据流详情）、`5.3-enhanced-measurement-open.png`（设置面板）、`5.3-enhanced-measurement-advanced.png`（高级设置展开，关键证据）

取证路径（实际执行）：GA4 → 切换到 TEYES 账号（387127793）→ TEYES 媒体资源（527825601）→ 管理 → 数据流 → 网站数据流 `teeyesglobal.com`（数据流 ID 13888212498，**衡量 ID 已核对 = G-VBNCN089DG** ✓）→ 增强型衡量 → 网页浏览量 → 显示高级设置。

**取证结果：**

| 项目 | 状态 |
|---|---|
| 增强型衡量功能总开关 | ✅ 启用中（正在衡量 7 项：网页浏览量、滚动次数、出站点击次数、网站搜索、表单互动次数、视频互动、文件下载次数） |
| 网页加载次数（checkbox） | ✅ 已勾选 |
| **根据浏览器历史记录事件判断的页面更改（checkbox）** | ✅ **已勾选（开启）** |

**结论**：SPA 浏览器历史记录触发的 page_view **已开启**——主站 `/contact` 等前端路由切换会自动产生 page_view 事件，GA4 侧无缺口，无需任何后续处理。

只查不改：全程未点击任何开关/复选框，未点「保存」，关闭面板退出。设置未发生任何变化。

---

## Part B · 5 — 人工发布检查单（GTM Preview 验证 4 项）

发布动作由用户人工执行（GTM 右上「提交」）。**Preview 验证前请先暂停广告拦截插件**（此前测试已遇到拦截器屏蔽 Google 代码的情况）。

| # | 验证场景 | 预期结果 |
|---|---|---|
| 1 | 主站任意页（如 `/contact`）提交联系表单 | 触发 `CE - form_submit_success - 主站` + 新 Ads 转化标签 `GAds Tracking \| 主站联系表单提交` + GA4 `generate_lead` |
| 2 | 批发页 `/android-car-stereo-wholesale/` 提交 | 只触发原 wholesale 转化标签；新主站触发器**不**触发 |
| 3 | OEM 页 `/android-car-stereo-oem-manufacturer/` 提交 | 只触发原 Manufacturer 转化标签；新主站触发器**不**触发 |
| 4 | 经销商页 `/teyes-android-car-stereo-distributor/` 提交（含 catalog 意向） | 只触发原 Distributor 转化标签；catalog 提交跳 `thank-you-catalog.html`、普通申请跳 `thank-you.html`，跳转不被新触发器误伤 |

发布后预期现象（非故障，无需处理）：
- Ads 转化列表中「主站联系表单提交」先显示**未验证/无效**：新标签随工作区发布并被真实访问后，最多约 3 小时转为「已验证」。
- Distributor 增强转化诊断显示「紧急/最近无数据」：该转化近 30 天转化次数为 0，没有提交自然没有诊断数据，属预期。
- 感谢页刷新不计转化：`form_submit_success` 只在 EmailJS 成功时推送一次（验收等式已在 PR #12 描述中）。

---

## 待用户决策 / 后续事项

1. ~~PR #12 合并~~ ✅ 已于 2026-08-05 合并部署（squash 提交 `ed9cba5`，生产部署 completed）。
2. ~~GA4 增强型衡量开关取证~~ ✅ 已补做（Part B · 4）：历史记录 page_view 开关为开启状态，无缺口。
3. **Catalog 是否从 TA1D 剥离**（Part B · 1）——等用户决策。
4. **GTM 工作区 `主站转化-v28` 人工发布**（按上方检查单，发布前建议先跑 Preview 4 项验证）。

---

## Part B · 6 — 发布前 Preview 验证结果（2026-08-05 晚，已完成 ✅）

### 验证环境（方法说明）

- 拦截根因已排除：NekoBox 代理「简易路由 → 阻止」中的 `geosite:category-ads-all` 规则会拦截 `googletagmanager.com` / `google-analytics.com`。用户删除该条后，两域名恢复连通（fetch 均 ok，GTM 正常加载）。
- Tag Assistant 会话中途失效后，改用**确定性验证法**：本地 HTTP 服务器（127.0.0.1:8899）托管 4 个测试页，页面路径与线上完全一致（`/`、`/android-car-stereo-wholesale/`、`/android-car-stereo-oem-manufacturer/`、`/teyes-android-car-stereo-distributor/`），通过官方代码段加载**预览环境 env-46 容器**（`gtm_auth` + `gtm_preview=env-46`），每页推送一次 `form_submit_success`，用 CDP 网络抓包判定实际发出的请求。
- 预览容器编译产物已存档：`handover-evidence/gtm-preview-env46.js`（含新标签、新触发器、contact_email_click，rules 编译正确：主站规则 = `form_submit_success` AND 不含三条落地页路径 → 触发 tag 8（GA4 generate_lead）+ tag 11（Ads I6gX））。

### 四场景实测结果（全部通过 ✅）

| # | 场景（页面路径） | 主站新转化 `I6gXCOuoitwcEKXJ0q9C` | 落地页原转化 | GA4 `generate_lead` | 判定 |
|---|---|---|---|---|---|
| 1 | `/`（主站） | **触发**（3 个请求：googleadservices/doubleclick） | — | **触发**（1） | ✅ |
| 2 | `/android-car-stereo-wholesale/` | **未触发**（0）✓ | `jdXlCMiivoYcEKXJ0q9C` 触发（3） | 触发（1） | ✅ |
| 3 | `/android-car-stereo-oem-manufacturer/` | **未触发**（0）✓ | `f-2YCNqyx4YcEKXJ0q9C` 触发（3） | 触发（1） | ✅ |
| 4 | `/teyes-android-car-stereo-distributor/` | **未触发**（0）✓ | `TA1DCMH4xoYcEKXJ0q9C` 触发（3） | 触发（1） | ✅ |

原始抓包存档：`net-main-scenario.json` / `net-wholesale-scenario.json` / `net-oem-scenario.json` / `net-distributor-scenario.json`。

**结论**：v28 工作区 5 项改动的核心逻辑全部符合预期——主站补齐 Ads 转化 + generate_lead，三个落地页严格隔离、零重复计数。可以发布。

### ⚠️ 发布前需用户人工确认的一项

- **增强转化（Enhanced Conversions）**：编译产物中 4 个表单转化标签的 `vtp_enableEnhancedConversionsCheckbox` 均为 **false**（即未生效）。用户 13:37 在 UI 勾选过 4 个标签的增强转化，但编译结果未体现，疑似勾选后保存未成功（当时 distributor 标签和主站新标签曾报问题）。
- 影响说明：增强转化只影响转化归因匹配精度，**不影响转化计数本身**。可以选择：(a) 发布前在 GTM 里重新勾选并保存 4 个标签的增强转化（自动收集）；或 (b) 先发布核心逻辑，之后再补增强转化。
- 建议：在 GTM 工作区打开 4 个表单转化标签逐一确认勾选状态，未勾的勾上并保存，然后再点「提交」。

---

## Part B · 7 — 发布确认（2026-08-05 20:50，已完成 ✅）

- 用户人工发布工作区 `主站转化-v28`（增强转化为新版 Google 代码级「允许包括用户提供的数据 + 自动检测」，默认开启，等价于旧版 4 标签逐一勾选，无需额外改动）。
- 发布后拉取线上容器 `gtm.js?id=GTM-MSPH5TMK` 验证（存档 `gtm-live-after-publish.js`，462 KB）：
  - 新转化标签 `I6gXCOuoitwcEKXJ0q9C` ✅ 已在线上
  - `contact_email_click` 事件标签 ✅ 已在线上
  - 原落地页标签（wholesale/OEM/distributor）✅ 保持完好
- **全部任务完成**。发布后预期（重申）：Ads「主站联系表单提交」最多约 3 小时从「未验证」转「已验证」（需有真实访问触发）；近 30 天 0 转化的 Distributor 诊断「最近无数据」属正常。

---

## v29 · catalog 从 TA1D 剥离（2026-08-05 23:00–次日 00:30，配置+验证完成，待人工发布）

### 背景

经销商落地页 `/teyes-android-car-stereo-distributor/` 的同一表单有两种提交意图（页面隐藏字段 `intent`，由 `scrollToForm('catalog'|'distributor')` 控制）：catalog 资料索取与经销商申请。此前两者都计入经销商转化 `TA1DCMH4xoYcEKXJ0q9C`，导致低意向 catalog 下载污染经销商转化数据。v29 把 catalog 拆成独立的**次要** Ads 转化。**页面代码零改动**（`public/teyes-android-car-stereo-distributor/index.html` 第 770 行本就会随事件推 `lead_type: 'catalog_request' | 'distributor_application'`，第 550/710–724 行 intent 逻辑确认）。

### 改动清单（全部在 GTM 工作区 `catalog拆分-v29` + Ads 后台，未发布）

| # | 位置 | 改动 | 证据 |
|---|---|---|---|
| 1 | Google Ads | 新建转化「catalog 资料索取（经销商页）」：类别=提交潜在客户表单、**次要操作**、计数=仅一次、点击窗口 90 天、归因=以数据为依据。Conversion ID `17816659109`，Label `Z-wbCKj1wNwcEKXJ0q9C` | `v29-ads-catalog-label.png` |
| 2 | GTM | 新建工作区 `catalog拆分-v29`（当前 env-50） | `v29-6.1-workspace-created.png` |
| 3 | GTM 变量 | 新建 `DLV - lead_type`（数据层变量，变量名 `lead_type`，版本 2） | `v29-6.2-dlv-lead-type.png` |
| 4 | GTM 触发器 | `Event Lead (Android-Car-Distributor)` 增加第二条件：`{{DLV - lead_type}}` 等于 `distributor_application`（原 Page Path 条件未动） | `v29-6.3-ta1d-condition-added.png` |
| 5 | GTM 触发器 | 新建 `CE - form_submit_success - catalog`：自定义事件 `form_submit_success`，某些自定义事件，条件 `{{Page Path}}` 包含 `/teyes-android-car-stereo-distributor/` + `{{DLV - lead_type}}` 等于 `catalog_request` | `v29-6.4-catalog-trigger-saved.png` |
| 6 | GTM 代码 | 新建 `GAds Tracking \| catalog 资料索取`（Google Ads 转化跟踪，ID `17816659109`，Label `Z-wbCKj1wNwcEKXJ0q9C`），触发条件挂 #5 | `v29-6.5-catalog-tag-saved.png` |

### 本地预览实测（env-50 预览包，全部通过 ✅）

预览包验证：`v29-preview-gtm.js`（465 KB）含 `Z-wbCK` / `catalog_request` / `distributor_application` / `lead_type` 各 1 处。

| 场景（推入 dataLayer 事件） | 期望 | 实测抓包 | 判定 |
|---|---|---|---|
| `form_submit_success` + `lead_type:'catalog_request'` | 只触发新 catalog 转化 | 3 个 conversion 请求全部 `label=Z-wbCKj1wNwcEKXJ0q9C`，**无 TA1D** | ✅ |
| `form_submit_success` + `lead_type:'distributor_application'` | 只触发原经销商转化 | 3 个 conversion 请求全部 `label=TA1DCMH4xoYcEKXJ0q9C`，**无 Z-wbCK**；GA4 collect 正常 1 条 | ✅ |

原始抓包存档：`v29-net-catalog-scenario.json` / `v29-net-distributor-scenario.json`。

### 待用户人工操作

- 在 GTM 打开工作区 `catalog拆分-v29`，点「提交」→「发布」（版本名建议 `catalog拆分-v29`）。发布即生效：catalog 下载计入新次要转化，经销商申请继续计入 TA1D 主转化，两者从此分开统计。

### v29 发布确认（2026-08-06 08:31，已完成 ✅）

- 用户人工发布工作区 `catalog拆分-v29`。
- 发布后拉取线上容器 `gtm.js?id=GTM-MSPH5TMK` 验证（存档 `gtm-live-after-v29.js`，464 KB）：
  - 新 catalog 转化标签 `Z-wbCKj1wNwcEKXJ0q9C` ✅ 已在线上
  - `catalog_request` / `distributor_application` / `lead_type` 过滤逻辑 ✅ 已在线上
  - 原经销商转化 `TA1DCMH4xoYcEKXJ0q9C` ✅ 保持完好
- **v29 全部任务完成**。发布后预期：Ads「catalog 资料索取（经销商页）」最多约 3 小时从「未验证」转「已验证」（需有真实 catalog 提交触发）；此后 catalog 与经销商申请分开统计。
